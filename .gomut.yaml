target:
  mode: package
  value: ./sample
kind:
  mode: standard
type:
  - lived
  - not-covered
#  enable:
#    - bitwise_operator
#  disable:
#    - return
progress: on
jsonl: mutations.jsonl
html: report.html
sarif: report.sarif
isolation:
  copy_exclude:
    - .codex
    - .worktrees
    - docs
    - internal/cache/**
    - mutations.jsonl
    - report.html
    - report.sarif
    - tmp
