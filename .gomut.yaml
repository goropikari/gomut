target:
  mode: package
  value: ./sample
kind:
  mode: standard
  enable:
    - bitwise_operator
  disable:
    - return
progress: on
jsonl: mutations.jsonl
html: report.html
isolation:
  copy_exclude:
    - .codex
    - .worktrees
    - docs
    - internal/cache/**
    - mutations.jsonl
    - report.html
    - tmp
